import { Ti, Type } from "@subsquid/substrate-metadata";
import { InkProject } from "./metadata/validator";
export interface SelectorsMap {
    [selector: string]: number;
}
export declare class AbiDescription {
    private project;
    constructor(project: InkProject);
    messageSelectors(): SelectorsMap;
    constructorSelectors(): SelectorsMap;
    messages(): Ti;
    constructors(): Ti;
    private createMessagesType;
    event(): Ti;
    private _types;
    private add;
    types(): Type[];
}
//# sourceMappingURL=abi-description.d.ts.map